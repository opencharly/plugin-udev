# 2026.266.1024 — feat: relocate the udev R10 witness bed from opencharly/charly

## Summary of changes

Relocate the `check-udev-local` R10 witness (and its `check-udev-app` `kind: local` template) out of `opencharly/charly`'s `charly.yml` and into `opencharly/plugin-udev` — the repo that owns the externalized `charly udev` COMMAND (`candy/plugin-udev`, `command:udev`). A check bed belongs beside the code it witnesses: the check-bed runner's dev-tree override (`proc.SelfSuperprojectOverridePair`) points the bed PROJECT's own superproject's `@github` refs at the local tree, and in the umbrella every submodule's superproject is `opencharly/opencharly` (which hosts no candies) — so a plugin candy owned by another repo was never exercised as in-development code while its witness sat in `charly`. Relocated here, the template composes the LOCAL `plugin-udev` candy with no `@github` pin.

- `charly.yml` (+49/−2) — adds the `check-udev-app` `kind: local` template (composing the LOCAL `plugin-udev` candy) and the `check-udev-local` disposable `host: local` bed, whose two GPU-less steps run `charly udev generate` (asserting the exact 99-charly-container-access rule lines) and `charly udev status` (exit 0, graceful device + rule report) host-side; migrates the `version:` stamp to the schema head (`2026.232.0520` → `2026.249.2125`); sweeps the now-false "declares no entities of its own" header comment.
- `.github/workflows/deploy.yml` (+1/−1) — bumps the CI-time charly source pin from `v2026.238.1242` to `v2026.256.1316`, so the CI gate's charly accepts the schema-head `version:` stamp.
- `.gitignore` (+16/−0) — new file; the standard generated-artifact rules (`.build/`, `.containerignore`, `.dockerignore`, `.check/`, `output/`, editor scratch, `*.bak*`).

## Change class

**candy/box-config** — `charly.yml` R10 bed declaration + CI pin bump + `.gitignore`; no runtime Go code changed.

**Closes / relates to:** relates to opencharly/charly#625 — the companion PR that removes this same witness from `opencharly/charly` in the same relocation. It must merge; a surviving duplicate is the R3 violation this move eliminates.

**Breaking change + rollback:** the bed/entity moves repos; rollback = revert (companion #625 restores the charly-side witness).

## How R10-tested

- **Gate for this change class:** `charly check run check-udev-local` + `charly box validate`.
- **Bed:** `check-udev-local` | **substrate:** local (`host: local`, `disposable: true`) | **ok:** true | **steps:** 6 | **seconds:** 27.
- **Binary:** `charly 2026.249.2125`.
- **Fresh clone at this tree:** `/tmp/opencode/rem-udev-4` at head `d0e92c3`, run with `CHARLY_REPO_OVERRIDE=github.com/opencharly/plugin-udev=/tmp/opencode/rem-udev-4` so the bed exercises THIS checkout's `candy/plugin-udev` (rebuilt at run time).
- **Changed path ran live:** yes — `check-live` executed both relocated steps (`udev-generate-rules`, `udev-status-runs`) with `exit=0`; `2 steps: 2 passed, 0 failed, 0 skipped`.
- **Fresh rebuild:** the bed's `update` stage ran the fresh rebuild and PASSED (`update: 3s ok=true`).
- **Concurrency:** N/A — single bed, one disposable deploy.

*1. Gate output (`charly box validate` + `charly check run check-udev-local`)*:

```
$ cd /tmp/opencode/rem-udev-4
$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-udev=/tmp/opencode/rem-udev-4" charly box validate
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly box validate: OK — checked 1 candy, 1 deploy, 7 distros, 7 builders; 0 warnings, 0 errors

$ CHARLY_REPO_OVERRIDE="github.com/opencharly/plugin-udev=/tmp/opencode/rem-udev-4" charly check run check-udev-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
plugin github.com/opencharly/plugin-deploy-local/candy/plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-deploy-local_candy_plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
Removed /home/atrawog/.config/containers/systemd/charly-check-udev-local.container
Reloaded systemd user daemon
charly check run check-udev-local: [deploy-add] START (charly deploy add; log: .check/check-udev-local/2026.266.1012/deploy-add.log)
charly check run check-udev-local: [deploy-add] PASS after 4.015s [allowlisted=1] (log: .check/check-udev-local/2026.266.1012/deploy-add.log)
charly check run check-udev-local: [bring-up-members] START
charly check run check-udev-local: [bring-up-members] PASS after 0s
charly check run check-udev-local: [check-live] START (charly check live; log: .check/check-udev-local/2026.266.1012/check-live.log)
charly check run check-udev-local: [check-live] PASS after 8.203s [allowlisted=1] (log: .check/check-udev-local/2026.266.1012/check-live.log)
charly check run check-udev-local: [update] START (charly update; log: .check/check-udev-local/2026.266.1012/update.log)
charly check run check-udev-local: [update] PASS after 3.282s [allowlisted=1] (log: .check/check-udev-local/2026.266.1012/update.log)
charly check run check-udev-local: [cleanup] START (charly remove; log: .check/check-udev-local/2026.266.1012/cleanup.log)
charly check run check-udev-local: [cleanup] PASS after 11.305s [allowlisted=1] (log: .check/check-udev-local/2026.266.1012/cleanup.log)
charly check run check-udev-local: [cleanup-members] START
charly check run check-udev-local: [cleanup-members] PASS after 0s
charly check run check-udev-local: PASS (steps=6)
```

`.check/check-udev-local/2026.266.1012/summary.yml`: `ok: true`, `total_seconds: 27`, `errors: 0`, `warnings: 0`; the sole finding is the allowlisted `dev-worktree-binary-notice` (informational dev-build behavior, suppressed 4× — not a defect).

*2. The changed code path executing live* (`check-live.log` — the relocated witness running against THIS checkout's `candy/plugin-udev`):

```
$ cat .check/check-udev-local/2026.266.1012/check-live.log
WARNING: charly is a dev/worktree build (not installed at /usr/bin or /usr/local/bin); NOT loading the installed package's plugins from /usr/lib/charly/plugins (they may be built against a different charly version). Set CHARLY_PLUGIN_DIR to load plugins from a custom location.
charly: resolving project (first run — may take a moment)...
Resolved @github.com/opencharly/plugin-deploy-local -> v2026.242.0527 (latest tag)
Resolved @github.com/opencharly/plugin-vm -> v2026.262.0752 (latest tag)
charly: fetching git metadata for 1 repo(s) (first run — may take a moment)...
charly: git metadata cached.
plugin plugin-deploy-local: served from /home/atrawog/.cache/charly/plugins/plugin-deploy-local-cb999434bd120951 (build stamp 1a5f05ee92f5d34d); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-deploy-local@v2026.242.0527/candy/plugin-deploy-local
Local deploy: check-udev-local [host (local)]
  PASS  check command=charly udev generate prints the exact 99-charly-container-access rule lines  [udev-generate-rules]  exit=0
  PASS  check command=charly udev status exits 0 and reports device + rule status (GPU-less graceful)  [udev-status-runs]  exit=0

2 steps: 2 passed, 0 failed, 0 skipped
```

*3. Docs-only evidence*:

```
N/A — not a docs-only change; the changed surface is a `charly.yml` bed declaration.
```

## Attribution tier

**fully tested and validated** — the relocated witness's full R10 sequence executed live against a fresh clone of this tree (`charly 2026.249.2125`), with the changed path (both relocated steps) passing on the fresh rebuild at zero warnings/zero errors.

## Harness rulebook compliance

- **R0 skills:** dispatcher rows matched before the first tool call — loaded `/charly-check:check` (bed model, R10), `/charly-internals:git-workflow` (PR-only landing, body-before-push), `/charly-internals:strict-policy` (R1–R5), `/charly-internals:root-cause-analyzer` (R1).
- **R1 RCA + zero warnings:** the block's findings were root-caused and remediated in this tree (body re-frozen to the template); `charly box validate` reports 0 warnings / 0 errors, the bed's `summary.yml` reports `errors: 0, warnings: 0`, and the only emitted warning is the allowlisted `dev-worktree-binary-notice`.
- **R2 no out-of-scope:** the source-side removal lands in the named companion `opencharly/charly#625`; nothing is parked as "follow-up".
- **R3 no duplication:** the witness has ONE canonical home — the plugin's owning repo; companion #625 deletes the charly-side duplicate.
- **R4 no workaround:** no sleep/retry/shim/magic-number; the witness runs the real `charly udev` command on the host.
- **R5 hard cutover:** the relocation IS the cutover; the charly-side entity is removed in `opencharly/charly#625` in the same move.
- **R6 git safety:** feature branch `feat/relocate-check-udev-local`; no force-push, no history rewrite; this body re-freeze is an empty append-only commit.
- **R7 runtime gate:** the end-to-end `charly check run check-udev-local` gate ran (not just `go test`), output pasted above.
- **R8 artifact invariants:** `N/A — no Containerfile-generation or ai.opencharly.* label change.`
- **R9 binary == source:** the bed ran on `charly 2026.249.2125` against the fresh clone; no new runtime OS dependency.
- **R10 disposable + coverage:** proven on a `disposable: true` `host: local` target; the witness IS the coverage that fails without the plugin's command dispatch.
- **RDD:** the dispatch premise was verified live — the run executed this checkout's `candy/plugin-udev` (rebuilt at run time) through `${CHARLY_BIN} udev`.
- **ADE:** the template composes the LOCAL `plugin-udev` candy; `charly box validate` reports 0 errors (1 candy, 1 deploy).
- **SDD:** `N/A — no .cue edit.`
- **Hard cutover:** one atomic config commit; companion removal is the matching leg; no Phase-2/TODO in scope.
- **Kernel/plugin boundary law:** the witness MOVES OUTWARD out of core (`opencharly/charly`) into the plugin's owning repo; no core/sdk movement.
- **Disposable-only autonomy:** the teardown/rebuild was on a `disposable: true` bed.
- **Clean architecture + Go gates:** `N/A for Go — no Go changed.`
- **CHANGELOG:** `N/A — this repo's tag-on-merge writes CHANGELOG/<CalVer>.md from this PR body at merge (org convention).`

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*